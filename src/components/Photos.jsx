let imagesDML = [];
for (let i = 1; i <= 18; i++) {
  imagesDML.push({ image: `./images/photos/DML${i}.jpg` });
}

let imagesPuncher = [];
for (let i = 1; i <= 48; i++) {
  imagesPuncher.push({ image: `./images/photos/puncher${i}.jpg` });
}

export { imagesDML, imagesPuncher };
